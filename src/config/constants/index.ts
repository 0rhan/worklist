/**
* HeadHunter default API address
* @readonly
**/
export  const BASE_URL = "https://api.hh.ru/" as const

export const LOADING = "LOADING" as const
export const IDLE = "IDLE" as const
export const ERROR = "ERROR" as const
export const SUCCESS = "SUCCESS" as const
